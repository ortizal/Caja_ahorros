spring:
  test:
    database:
      replace: none
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none

app:
  jwt:
    secret: ${JWT_SECRET:test-secret-for-unit-tests-only-min-32-bytes}
